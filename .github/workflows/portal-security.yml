name: Portal security checks

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: portal-security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm

      - name: Install exact dependencies
        run: npm ci

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Verify source and security invariants
        run: npm run check

      - name: Build deployment allowlist
        run: npm run build

      - name: Prove internal files are excluded
        shell: bash
        run: |
          test ! -e dist/AGENTS.md
          test ! -e dist/supabase
          test ! -e dist/docs
          test ! -e dist/tools
          test ! -e dist/gas-order-script.js
